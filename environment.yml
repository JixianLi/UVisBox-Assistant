name: chatuvisbox
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy>=1.26.0
  - matplotlib>=3.8.0
  - pandas>=2.2.0
  - pip
  - pip:
      # UVisBox and dependencies not in conda
      - uvisbox>=0.1.0
      - langchain-google-genai>=2.0.4
      - langgraph>=0.2.53
